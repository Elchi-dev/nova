// Nova Code Generation
//
// Targets:
// - LLVM IR for optimized binary output (nova build)
// - Interpreted mode for rapid development (nova run)
//
// The codegen respects module boundaries established by semantic
// analysis, emitting each module as a separate compilation unit
// to enable hot-reloading at runtime.

/// Placeholder — codegen implementation coming soon
pub fn generate(_program: &crate::ast::Program) -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
