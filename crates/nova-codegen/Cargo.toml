[package]
name = "nova-codegen"
version.workspace = true
edition.workspace = true
description = "LLVM code generation backend for Nova"

[dependencies]
nova-compiler = { path = "../nova-compiler" }
inkwell = { version = "0.9.0", features = ["llvm20-1"] }
thiserror = "2"
